import { NullDefaultValueDirectiveDirective } from './null-default-value-directive.directive';

describe('NullDefaultValueDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new NullDefaultValueDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
